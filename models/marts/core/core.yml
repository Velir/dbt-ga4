version: 2

models:
  - name: dim_sessions
    description: Inventory of all sessions with accompanying information. Unique on session_key
    columns:
      - name: session_key
        tests:
          - unique
