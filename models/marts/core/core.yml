version: 2

models:
  - name: dim_ga4__sessions
    description: Inventory of all sessions with accompanying dimension information. Unique on session_key
    columns:
      - name: session_key
        tests:
          - unique
