version: 2

models:
  - name: stg_ga4__event_scroll
    description: >
      Staging model containing only 'scroll' events. 
      Includes the 'percent_scrolled' parameter.
      GA4's default implementation will only track the 90% scroll threshold
